function Logo() {
  return (
    <div className="header-logo-container">
      <img
        className="header-logo"
        alt="Logo"
        src={require("../../../images/logo.png")}
      />
    </div>
  );
}

export default Logo;
